rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY TESTING RULES - ALLOW ALL ACCESS
    // Use this ONLY for testing, then switch back to firestore.rules
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
